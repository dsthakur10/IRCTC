
Run init.sh to create essential database files for our IRCTC database:
    >sh init.sh 

Compile & run the server:
	> gcc server.c -o server
    	>./server

Compile & run the client:
	> gcc client.c -o client
    	>./client

first sign up and create one admin account. 

The secret pin for admin account is "root10".

now login into admin account and add trains.

create customers/agents and login to book ticket.

